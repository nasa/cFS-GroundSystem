name: Static Analysis

on:
  push:
    branches:
      - dev
      - main
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
  workflow_dispatch:

permissions:
  contents: read

jobs:

  static-analysis:
    name: Run cppcheck
    uses: nasa/cFS/.github/workflows/static-analysis-reusable.yml@dev
